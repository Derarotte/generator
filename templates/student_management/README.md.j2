# {{ project_name_cn }}

> 自动生成于: {{ _generated_at }}  
> 作者: {{ author }}

## 📋 项目简介

这是一个基于 **Spring Boot + Vue 3 + MySQL** 的{{ project_name_cn }}，提供完整的学生信息管理功能。

## 🛠️ 技术栈

### 后端
- Java 11
- Spring Boot 2.7.18
- MyBatis
- MySQL 8.0

### 前端
- Vue 3
- Vite
- Element Plus
- Axios

## 📦 功能模块

{% for feature in features %}
- ✅ {{ feature | capitalize }}管理
{% endfor %}

## 🚀 快速开始

### 1. 环境要求

- JDK 11+
- Maven 3.6+
- Node.js 18+
- MySQL 8.0+

### 2. 数据库初始化

```bash
mysql -u {{ db_username | default('root') }} -p < database/init.sql
```

### 3. 启动后端

```bash
cd backend
mvn spring-boot:run
```

后端默认运行在 http://localhost:{{ server_port | default(8080) }}

### 4. 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端默认运行在 http://localhost:5173

## 📁 项目结构

```
{{ project_name }}/
├── backend/                    # Spring Boot 后端
│   ├── src/main/java/
│   │   └── {{ package_path }}/
│   │       ├── controller/     # 控制器
│   │       ├── service/        # 服务层
│   │       ├── mapper/         # MyBatis Mapper
│   │       ├── entity/         # 实体类
│   │       └── Application.java
│   ├── src/main/resources/
│   │   └── application.yml     # 配置文件
│   └── pom.xml
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── views/
│   │   ├── components/
│   │   └── main.js
│   └── package.json
├── database/                   # 数据库脚本
│   └── init.sql
└── docs/                       # 文档
    └── 实验报告.md
```

## 📝 API 接口

### 学生管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/student/list | 获取学生列表 |
| GET | /api/student/{id} | 获取单个学生 |
| POST | /api/student/save | 新增/更新学生 |
| DELETE | /api/student/{id} | 删除学生 |
| POST | /api/student/batchDelete | 批量删除 |

## 📄 License

MIT License

---

*此项目由 [中国学生作业代码生成器](https://github.com/Derarotte/generator) 自动生成*
