# 开发者指南

本文档面向希望参与本项目开发的开发者，包含项目架构、开发规范和工作流程。

---

## 目录

1. [项目架构](#项目架构)
2. [目录结构](#目录结构)
3. [核心模块说明](#核心模块说明)
4. [开发环境搭建](#开发环境搭建)
5. [开发工作流](#开发工作流)
6. [如何添加新模块](#如何添加新模块)
7. [代码规范](#代码规范)
8. [测试规范](#测试规范)
9. [发布流程](#发布流程)
10. [常见问题](#常见问题)

---

## 项目架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      用户界面层                              │
│  ┌─────────────────┐              ┌─────────────────┐       │
│  │   Web 前端       │              │    CLI 工具     │       │
│  │   (Vue 3)       │              │   (Python)      │       │
│  └────────┬────────┘              └────────┬────────┘       │
└───────────┼────────────────────────────────┼────────────────┘
            │                                │
            ▼                                ▼
┌─────────────────────────────────────────────────────────────┐
│                       API 层 (FastAPI)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │ /modules │  │/generator│  │/templates│  │/internal │     │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘     │
└───────┼─────────────┼─────────────┼─────────────┼───────────┘
        │             │             │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────┐
│                      核心引擎层                              │
│  ┌─────────────────┐  ┌─────────────────┐                   │
│  │  TemplateLoader │  │ GeneratorEngine │                   │
│  │  (加载YAML配置)  │  │  (渲染+打包)    │                   │
│  └────────┬────────┘  └────────┬────────┘                   │
└───────────┼────────────────────┼────────────────────────────┘
            │                    │
            ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│                       模板层                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ module.yaml  │  │  *.j2 模板   │  │   静态资源    │       │
│  │ (模块定义)    │  │ (Jinja2)     │  │              │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 核心设计原则

1. **模板驱动** - 所有生成逻辑由模板定义，引擎只负责渲染
2. **插件化** - 模块可独立开发和添加，无需修改核心代码
3. **配置声明式** - 使用 YAML 声明模块结构，而非硬编码
4. **前后端分离** - API 与 UI 解耦，支持多种客户端

---

## 目录结构

```
generator/
├── backend/                    # 后端代码
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py            # FastAPI 入口
│   │   ├── config.py          # 配置管理
│   │   ├── api/               # API 路由
│   │   │   ├── generator.py   # 生成相关 API
│   │   │   ├── modules.py     # 模块查询 API
│   │   │   ├── templates.py   # 模板管理 API
│   │   │   └── internal.py    # 内部测试 API
│   │   ├── core/              # 核心引擎
│   │   │   ├── engine.py      # 生成引擎
│   │   │   ├── template_loader.py  # 模板加载器
│   │   │   └── modules/       # 预留扩展
│   │   └── utils/             # 工具函数
│   │       └── logger.py      # 日志系统
│   └── requirements.txt
│
├── frontend/                   # 前端代码
│   ├── src/
│   │   ├── main.js
│   │   ├── App.vue
│   │   ├── router/
│   │   └── views/
│   ├── package.json
│   └── vite.config.js
│
├── templates/                  # 【核心】模板库
│   └── {module_id}/           # 每个模块一个目录
│       ├── module.yaml        # 模块配置 (必需)
│       ├── backend/           # 后端模板
│       ├── frontend/          # 前端模板
│       ├── database/          # 数据库模板
│       └── docs/              # 文档模板
│
├── docs/                       # 项目文档
│   ├── api-reference.md       # API 文档
│   ├── module-development.md  # 模块开发指南
│   ├── deployment.md          # 部署指南
│   └── DEVELOPER.md           # 开发者指南 (本文件)
│
├── README.md                   # 项目说明
├── CHANGELOG.md               # 更新日志
├── ROADMAP.md                 # 开发路线图
└── cli.py                     # 命令行工具
```

---

## 核心模块说明

### 1. TemplateLoader (`backend/app/core/template_loader.py`)

**职责**: 扫描 `templates/` 目录，解析 `module.yaml`，加载模块定义

**关键类**:
- `ModuleDefinition` - 模块定义数据结构
- `FieldDefinition` - 字段定义
- `FileMapping` - 文件映射规则
- `TemplateLoader` - 加载器主类

**扩展点**:
- 添加新的字段类型支持
- 添加模块验证逻辑

### 2. GeneratorEngine (`backend/app/core/engine.py`)

**职责**: 根据模块定义和用户配置，渲染模板生成项目

**关键流程**:
```
1. 获取模块定义
2. 合并默认配置和用户配置
3. 创建输出目录
4. 遍历文件映射，渲染每个模板
5. 打包为 ZIP
```

**扩展点**:
- 添加后处理钩子（如格式化代码）
- 添加生成前验证

### 3. API Layer (`backend/app/api/`)

**路由**:
| 路由 | 文件 | 说明 |
|------|------|------|
| `/api/modules` | modules.py | 模块查询 |
| `/api/generator` | generator.py | 生成和下载 |
| `/api/templates` | templates.py | 模板管理 |
| `/api/internal` | internal.py | 开发测试 |

---

## 开发环境搭建

### 1. 克隆项目

```bash
git clone https://github.com/Derarotte/generator.git
cd generator
```

### 2. 后端环境

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

### 3. 前端环境

```bash
cd frontend
npm install
```

### 4. 启动开发服务器

```bash
# 终端1: 后端
cd backend
uvicorn app.main:app --reload --port 8000

# 终端2: 前端
cd frontend
npm run dev
```

### 5. 验证

- API 文档: http://localhost:8000/docs
- 前端: http://localhost:3000
- 健康检查: http://localhost:8000/api/health

---

## 开发工作流

### Git 分支策略

```
main          # 稳定版本，只接受 PR
├── develop   # 开发分支
├── feature/* # 功能分支
├── bugfix/*  # 修复分支
└── release/* # 发布分支
```

### 提交规范

使用 [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: 添加图书管理系统模块
fix: 修复模板渲染路径问题
docs: 更新 API 文档
refactor: 重构生成引擎
test: 添加单元测试
chore: 更新依赖
```

### 工作流程

1. **创建分支**: `git checkout -b feature/library-module`
2. **开发**: 编写代码和测试
3. **本地测试**: 确保所有测试通过
4. **提交**: `git commit -m "feat: 添加图书管理模块"`
5. **推送**: `git push origin feature/library-module`
6. **PR**: 创建 Pull Request 到 develop

---

## 如何添加新模块

### 步骤 1: 创建模块目录

```bash
mkdir -p templates/library_management/{backend,frontend,database,docs}
```

### 步骤 2: 编写 module.yaml

```yaml
# templates/library_management/module.yaml
id: library_management
name: 图书管理系统
description: 基于 Spring Boot 的图书借阅管理系统
version: "1.0.0"
icon: 📚
category: 管理系统

tech_stack:
  - Spring Boot
  - Vue 3
  - MySQL

fields:
  - name: project_name
    label: 项目名称
    type: text
    default: LibrarySystem
    
  - name: package_name
    label: 包名
    type: text
    default: com.example.library

files:
  - source: backend/pom.xml.j2
    target: backend/pom.xml
  # ... 更多文件映射
```

### 步骤 3: 编写模板文件

使用 Jinja2 语法:
```java
// templates/library_management/backend/Application.java.j2
package {{ package_name }};

@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

### 步骤 4: 测试

```bash
curl -X POST http://localhost:8000/api/generator/generate \
  -H "Content-Type: application/json" \
  -d '{"module_id": "library_management"}'
```

### 步骤 5: 验证生成的项目

```bash
# 解压并检查
unzip output/{project_id}.zip -d test_output
# 验证能否编译/运行
cd test_output/backend && mvn compile
```

---

## 代码规范

### Python (后端)

- 使用 PEP 8 风格
- 使用 Type Hints
- 文档字符串使用 Google 风格
- 最大行长度: 100

```python
def generate_project(
    module_id: str,
    config: Dict[str, Any]
) -> GenerationResult:
    """
    生成项目。
    
    Args:
        module_id: 模块ID
        config: 用户配置
        
    Returns:
        GenerationResult: 生成结果
    """
    pass
```

### JavaScript/Vue (前端)

- 使用 ESLint + Prettier
- 组件使用 `<script setup>` 语法
- Props 必须声明类型

### 模板文件

- 使用 `.j2` 后缀
- 变量使用 `{{ variable }}`
- 控制语句使用 `{% if %}` `{% for %}`
- 保持生成文件的格式整洁

---

## 测试规范

### 单元测试

```bash
cd backend
pytest tests/
```

### 集成测试

```bash
python test_e2e.py
```

### 测试检查清单

- [ ] 模板加载正常
- [ ] API 返回正确
- [ ] 生成文件完整
- [ ] ZIP 可正常解压
- [ ] 生成的项目可编译

---

## 发布流程

### 1. 更新版本号

- `backend/app/config.py`: `APP_VERSION`
- `CHANGELOG.md`: 添加版本记录

### 2. 创建 Release 分支

```bash
git checkout -b release/v1.1.0
```

### 3. 测试

```bash
# 运行所有测试
pytest
python test_e2e.py
```

### 4. 合并到 main

```bash
git checkout main
git merge release/v1.1.0
```

### 5. 打标签

```bash
git tag -a v1.1.0 -m "v1.1.0 - xxx"
git push --tags
```

### 6. GitHub Release

在 GitHub 创建 Release，附上 CHANGELOG 内容。

---

## 常见问题

### Q: 添加新字段类型怎么做?

修改 `FieldDefinition` 类和前端表单组件。

### Q: 如何调试模板渲染?

启用 DEBUG 日志:
```python
# config.py
LOG_LEVEL = "DEBUG"
```

### Q: 生成的文件乱码?

确保模板文件使用 UTF-8 编码。

### Q: 如何支持新的技术栈?

创建新的模块模板，技术栈是模板层面的事情，引擎无需修改。

---

## 联系方式

- GitHub Issues: https://github.com/Derarotte/generator/issues
- 作者: @Derarotte

---

*最后更新: 2026-01-20*
